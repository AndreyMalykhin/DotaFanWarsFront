import React from 'react';

const ChatInput = React.createClass({
    propTypes: {

    },

    render() {
        return (
            <div>ChatInput</div>
        );
    }
});

export default ChatInput;
